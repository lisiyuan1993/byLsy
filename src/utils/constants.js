import utils from '@cdn/utils';
/**
 * 凭证
 * @type {String}
 */
const TOKEN = "token";

const PHONE = "phone";

/**
 * openid
 * @type {String}
 */
const OPENID = "openid";
    

/**
 * webview
 * @type {Object}
 */
const WEBVIEWURL = "webviewurl";

export default {
    TOKEN,
    WEBVIEWURL,
    PHONE, OPENID
}
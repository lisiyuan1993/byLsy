import wepy from 'wepy';
import utils from '@cdn/utils';
import store from '@cdn/utils/storage';
import api from '@api';
import constants from '@cdn/utils/constants';
let cdn = utils.cdn;
export {
    wepy,
    utils,
    store,
    api,
    constants,
    cdn
}